Improve previously-incomplete guards against zstandard compression dictionary training failures in `butler aggregate-graph`.

Dictionary training can fail if the sample count is too small, not just if the total sample size is too large.
